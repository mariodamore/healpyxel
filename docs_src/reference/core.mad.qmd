# core.mad { #healpyxel.core.mad }

```python
core.mad(arr)
```

Compute Median Absolute Deviation.

Args:
    arr: Input array

Returns:
    MAD value (float)

Examples:
    >>> arr = np.array([1, 2, 3, 4, 5])
    >>> mad(arr)
    1.0