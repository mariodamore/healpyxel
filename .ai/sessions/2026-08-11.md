# Session: 2026-08-11

## Goals
- [ ] Draft ADR-015 for multi-resolution sidecar optimization

## Progress
- Analyzed current `sidecar.run()` nside loop (lines 1494-1568): recomputes full geometric
  pipeline per nside, discarding work
- Confirmed PSF is implemented in sidecar for point sources (weight=1.0 always) and for
  fuzzy polygon mode (centroid-to-cell centroid distance). PSF weights are aggregatable
  via bit-shift because NEST parent-child is a right-shift.
- Confirmed NEST ordering is used everywhere (nest=True in all index calls)
- Drafted ADR-015 with algorithm, PSF behavior, alternatives, and consequences

## Dead Ends
- None

## Decisions Made
- ADR-015: Multi-resolution sidecar via NEST bit-shift aggregation — drafted, pending
  implementation

## Carry-over
- [ ] Implement ADR-015: modify `sidecar.run()` to compute highest nside once, aggregate
  lower nsides via bit-shift. Key implementation points:
  - Sort `nsides` descending, compute only for first (highest)
  - `parent_shift = 2 * log2(nside_max / nside_i)` per lower nside
  - Group/aggregate: sum weights per (source_id, parent_hid)
  - Handle `no_psf_normalize` (stride vs sum for normalization)
  - Write same output paths and metadata as before
- [ ] Add tests in test_sidecar.py (or equivalent) comparing bit-shift output to full
  recompute for identical-ness on multi-nside invocations
