stages:
  - deploy

pages:
  stage: deploy
  script:
    - mkdir -p public
    - cp -r docs/* public/
  artifacts:
    paths:
      - public
  only:
    - master
  tags:
      - psl  # <--- Use 'psl' if you want the shell runner
