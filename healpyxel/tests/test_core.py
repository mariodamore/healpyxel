def collect_sidecar_outputs(input_path, tmp, read_stats=False):
    # Implementation of the function goes here
    pass

# Test case for collect_sidecar_outputs
def test_collect_sidecar_outputs():
    input_path = "path/to/input"
    tmp = "path/to/tmp"
    result = collect_sidecar_outputs(input_path, tmp, read_stats=False)
    assert result is not None  # Replace with actual expected result

# Run the test
test_collect_sidecar_outputs()